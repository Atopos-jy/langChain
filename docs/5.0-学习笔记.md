# LangChain.js 5.0 学习笔记：Agent 智能代理 🤖

> **学习时间**：2026-06-10  
> **核心主题**：从手动 ReAct 到原生 Tool Calling，再到 createAgent 官方封装

---

## 一、5.0 学习路径总览

| 步骤 | 文件                            | 主题                             | 状态    |
| ---- | ------------------------------- | -------------------------------- | ------- |
| ①    | `react-agent.ts`                | 手动版 ReAct（正则解析工具调用） | ✅ 已学 |
| ②    | `tool-calling-agent.ts`         | 原生 Tool Calling（bindTools）   | ✅ 已学 |
| ③    | `react-agent-plus.ts`           | createAgent 官方封装             | ✅ 已学 |
| ④    | `plan-and-execute.ts`           | 先计划再执行                     | ⏳ 待学 |
| ⑤    | `plan-and-execute-langgraph.ts` | LangGraph 图结构 Agent           | ⏳ 待学 |

---

## 二、核心概念

### 2.1 什么是 Agent（智能代理）

Agent 是一种让 LLM **自主决策**的编程范式——不只是"问一句答一句"，而是给模型工具，让它自己决定"下一步做什么"。

**传统 LLM 调用：**

```
用户输入 → 模型回答（一次调用，结束）
```

**Agent 模式：**

```
用户输入 → 模型思考 → 决定行动 → 执行工具 → 观察结果 → 再思考 → ... → 最终回答
```

### 2.2 ReAct 模式（Reasoning + Acting）

**核心流程：**

```
Thought（思考） →  Action（行动） →  Observation（观察） →  Thought → ... →  Final Answer
```

- **Thought**：模型分析当前情况，决定下一步做什么
- **Action**：模型选择工具并传入参数
- **Observation**：工具执行后的返回结果（由系统填入）
- **Final Answer**：模型认为信息足够后给出的最终回答

### 2.3 Tool Calling（原生工具调用）

LLM API 原生支持的"工具调用"能力，不是通过文本解析实现的。

**关键 API：**

```typescript
const llmWithTools = llm.bindTools(tools);
```

调用时，API 请求会带上工具定义：

```json
{
  "model": "...",
  "tools": [{
    "type": "function",
    "function": {
      "name": "search",
      "description": "搜索网络信息",
      "parameters": { "type": "object", "properties": {...} }
    }
  }]
}
```

模型返回结构化 `tool_calls`，不需要正则解析。

### 2.4 三种 Agent 实现方式对比

| 实现方式         | 核心文件                | 工具调用方式               | 循环控制          | 可靠性      | 代码量   |
| ---------------- | ----------------------- | -------------------------- | ----------------- | ----------- | -------- |
| **手动 ReAct**   | `react-agent.ts`        | Prompt 引导 + 正则解析文本 | 手写 `while` 循环 | ❌ 格式易错 | ~50 行   |
| **Tool Calling** | `tool-calling-agent.ts` | `bindTools` + 结构化返回   | 手写 `for` 循环   | ✅ 可靠     | ~30 行   |
| **createAgent**  | `react-agent-plus.ts`   | `createAgent` 封装         | 框架自动处理      | ✅✅        | **3 行** |

### 2.5 手动 ReAct vs Tool Calling（核心区别）

```
手动 ReAct：
  模型输出文本 "Action: search[北京天气]"
  → 你用 /Action:\s*(\w+)\[(.*?)\]/ 正则解析
  → 取出 "search" 和 "北京天气"

Tool Calling：
  模型 API 直接返回：
  { tool_calls: [{ name: "search", args: { query: "北京天气" } }] }
  → 你直接读 toolCall.name 和 toolCall.args
  → 无需解析
```

### 2.6 关键组件

| 组件            | 来源                       | 作用                                |
| --------------- | -------------------------- | ----------------------------------- |
| `tool()`        | `@langchain/core/tools`    | 定义工具（名称、描述、参数 Schema） |
| `bindTools()`   | `ChatOpenAI` 实例方法      | 将工具注册到 LLM（API 级别）        |
| `ToolMessage`   | `@langchain/core/messages` | 工具调用结果的标准消息类型          |
| `createAgent()` | `langchain`                | 官方 Agent 构建函数（封装循环）     |
| `z` (Zod)       | `zod`                      | 声明参数结构，类型安全              |

---

## 三、代码模板

### 3.1 定义工具（通用模板）

```typescript
import { tool } from "@langchain/core/tools";
import { z } from "zod";

const myTool = tool(
    async ({ param }: { param: string }) => {
        // 工具执行逻辑（查数据库、调 API、算数学……）
        const result = await doSomething(param);
        return result;
    },
    {
        name: "my_tool", // 工具名称（模型用这个名字来调用）
        description: "工具描述，告诉模型什么时候用它", // 模型根据描述选择工具
        schema: z.object({
            // 参数结构（模型按此生成参数）
            param: z.string().describe("参数说明"),
        }),
    },
);
```

### 3.2 Tool Calling Agent（手写循环）

```typescript
const llmWithTools = llm.bindTools([tool1, tool2]);
const messages = [new HumanMessage(question)];

for (let i = 0; i < maxIterations; i++) {
    const response = await llmWithTools.invoke(messages);

    // 没有工具调用 → 有最终答案了
    if (!response.tool_calls || response.tool_calls.length === 0) {
        return response.content;
    }

    messages.push(response);

    for (const toolCall of response.tool_calls) {
        const result = await toolsByName[toolCall.name].invoke(toolCall.args);
        messages.push(
            new ToolMessage({ content: result, tool_call_id: toolCall.id }),
        );
    }
}
```

### 3.3 createAgent（官方封装）

```typescript
import { createAgent } from "langchain";

const agent = createAgent({
    model: llm,
    tools: [searchTool, calculatorTool],
    systemPrompt: "你是一个智能助手，善于使用工具来回答问题。",
});

const result = await agent.invoke({
    messages: [{ role: "user", content: question }],
});

const answer = result.messages[result.messages.length - 1].content;
```

---

## 四、常见坑点

### ❌ 坑 1：手动 ReAct 的格式解析失败

模型可能输出 `Action:search[北京天气]`（少空格）或 `Action：搜索[北京天气]`（中文冒号），导致正则 `/Action:\s*(\w+)\[(.*?)\]/` 匹配失败。

**解决方案**：改用 Tool Calling（`bindTools`），不需要解析文本。

### ❌ 坑 2：Tool Calling 不支持所有模型

`bindTools` 依赖模型的 Function Calling 能力。DeepSeek 支持，但一些小型或旧模型可能不支持。

**解决方案**：手动 ReAct 作为 fallback。

### ❌ 坑 3：工具无限循环

Agent 可能一直在调用工具而不给出最终答案。

**解决方案**：设置 `maxIterations` 上限（通常 5-10 步）。

### ❌ 坑 4：ToolMessage 缺少 tool_call_id

```typescript
// ❌ 错误
new ToolMessage({ content: result });

// ✅ 正确
new ToolMessage({ content: result, tool_call_id: toolCall.id });
```

没有 `tool_call_id`，模型无法将工具结果与对应的调用关联起来。

---

## 五、Agent 工作流图解

```
┌─────────────────────────────────────────────────────────────┐
│                    用户输入问题                               │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │  LLM 分析问题    │
              │  决定下一步行动  │
              └────────┬────────┘
                       │
              ┌────────▼────────┐
              │  需要调用工具？  │
              └────────┬────────┘
                       │
            ┌──────────┴──────────┐
            ▼                     ▼
     ┌──────────────┐    ┌──────────────┐
     │  执行工具     │    │  生成最终答案 │
     │  返回结果     │    │  返回给用户   │
     └──────┬───────┘    └──────────────┘
            │
            ▼
     ┌──────────────┐
     │  结果注入消息 │
     │  继续循环     │
     └──────┬───────┘
            │
            ▼
     回到 LLM 分析
```

---

## 六、学习进度追踪

### 学习进度总览表

| 模块           | 子组件                          | 掌握程度 | 掌握度 | 最后实践日期 | 待加强点     |
| -------------- | ------------------------------- | -------- | ------ | ------------ | ------------ |
| **Agent 基础** | ReAct 模式概念                  | 熟练     | ⭐⭐⭐ | 2026-06      | -            |
| **Agent 基础** | 手动 ReAct 实现                 | 熟练     | ⭐⭐⭐ | 2026-06      | -            |
| **Agent 基础** | Thought/Action/Observation 循环 | 熟练     | ⭐⭐⭐ | 2026-06      | -            |
| **Agent 基础** | Tool Calling（bindTools）       | 熟练     | ⭐⭐⭐ | 2026-06      | -            |
| **Agent 基础** | createAgent 官方封装            | 会用     | ⭐⭐   | 2026-06      | 内部机制理解 |
| **Agent 进阶** | Plan-and-Execute                | 未学     | ☆      | -            | 待学习       |
| **Agent 进阶** | LangGraph Agent                 | 未学     | ☆      | -            | 待学习       |
| **Tool 定义**  | tool() + Zod Schema             | 熟练     | ⭐⭐⭐ | 2026-06      | -            |
| **Tool 定义**  | ToolMessage 使用                | 会用     | ⭐⭐   | 2026-06      | -            |

### 知识盲区

| 盲区                 | 说明                                     |
| -------------------- | ---------------------------------------- |
| Plan-and-Execute     | 先计划再执行模式，还没接触               |
| LangGraph            | 用图结构管理 Agent 状态流，还没接触      |
| 真实 API 工具集成    | 目前工具里是模拟数据，没接过真实 API     |
| createAgent 内部实现 | 知道怎么用，但不清楚它内部怎么封装循环的 |

---

## 七、核心问题清单

| 优先级 | 问题                                                                        | 需要复习 |
| ------ | --------------------------------------------------------------------------- | -------- |
| 🔴 高  | ReAct 模式的四个核心步骤是什么？（Thought/Action/Observation/Final Answer） | ☐        |
| 🔴 高  | 手动 ReAct 和 Tool Calling 的本质区别是什么？                               | ☐        |
| 🔴 高  | `bindTools` 做了什么？它和"把工具写在 Prompt 里"有什么区别？                | ☐        |
| 🔴 高  | Tool Calling 的 `tool_calls` 数据是怎样从模型传到你代码中的？               | ☐        |
| 🟡 中  | 为什么 ToolMessage 需要 `tool_call_id`？                                    | ☐        |
| 🟡 中  | createAgent 帮你封装了哪些手写逻辑？                                        | ☐        |
| 🟡 中  | 工具定义中的 `description` 字段起什么作用？                                 | ☐        |
| 🟡 中  | Tool Calling 适合什么模型？什么情况下还是得用手动 ReAct？                   | ☐        |
| 🟢 低  | Agent 循环的最大步数设多少合适？不设会怎样？                                | ☐        |
| 🟢 低  | Zod Schema 在 Tool Calling 中起什么作用？                                   | ☐        |

---

## 八、下一步学习建议

### 明天继续

```
④ plan-and-execute.ts       → 先计划再执行（多步骤任务）
⑤ plan-and-execute-langgraph.ts → LangGraph 图结构 Agent
```

### 推荐实战项目

学完 5.0 全部内容后可以尝试：

| 难度     | 项目                                                             | 涉及技术         |
| -------- | ---------------------------------------------------------------- | ---------------- |
| ⭐⭐     | **AI 计算器**：自然语言问数学题，自动调用计算工具                | Tool Calling     |
| ⭐⭐⭐   | **多工具客服机器人**：查天气 + 查数据库 + 发邮件，模型自动选工具 | Agent + 真实 API |
| ⭐⭐⭐⭐ | **研究报告生成器**：搜索 → 整理 → 写报告，多步规划               | Plan-and-Execute |

---

_明天继续：plan-and-execute.ts → LangGraph_
